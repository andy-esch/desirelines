// Code generated by schemas/bigquery/generate_proto.py. DO NOT EDIT.
// Source: schemas/bigquery/activities_full.json
//
// Mirrors the BigQuery `activities` table schema for the Pub/Sub
// BigQuery subscription (use_topic_schema), plus the CDC
// pseudocolumns. Timestamps are strings here, not micros: the
// subscription accepts a string for a TIMESTAMP column, so the
// producer forwards Strava's RFC 3339 values unchanged.
//
// Field numbering is sequential per-message; consumers match by name,
// not number, so reordering is safe.
//
// Syntax is proto2 (NOT proto3) because Storage Write rejects
// descriptors carrying the [proto3_optional=true] annotation that
// proto3's `optional` keyword emits. Every non-repeated field is
// labeled `optional`; BQ enforces REQUIRED at insert time.
//
// Regenerate: just generate-bq-proto    Verify in CI: just verify-schemas

syntax = "proto2";

package desirelines.bigquery.cdc.v1;

// Generates Go; the Pants target sets a matching `go_mod_address`.
option go_package = "github.com/andy-esch/desirelines/packages/dispatcher/types/generated";

message ActivityRow {
  message Athlete {
    // Athlete ID
    optional int64 id = 1;
    // Resource state
    optional int64 resource_state = 2;
  }

  message Map {
    // Map ID
    optional string id = 1;
    // Encoded polyline (not available from list endpoint)
    optional string polyline = 2;
    // Resource state
    optional int64 resource_state = 3;
    // Summary encoded polyline
    optional string summary_polyline = 4;
  }

  message Photos {
    message Primary {
      // Photo ID
      optional string id = 1;
      // Media type
      optional int64 media_type = 2;
      // Photo source
      optional int64 source = 3;
      // Unique photo ID
      optional string unique_id = 4;
      // Photo URLs as JSON
      optional string urls = 5; // BQ JSON — JSON-encoded string
    }

    // Primary photo
    optional Primary primary = 1;
    // Total photo count
    optional int64 count = 2;
  }

  message Gear {
    // Gear ID
    optional string id = 1;
    // Whether this is primary gear
    optional bool primary = 2;
    // Gear name
    optional string name = 3;
    // Resource state
    optional int64 resource_state = 4;
    // Total distance on gear
    optional double distance = 5;
  }

  message SegmentEfforts {
    message Activity {
      optional int64 id = 1;
      optional int64 resource_state = 2;
    }

    message Athlete {
      optional int64 id = 1;
      optional int64 resource_state = 2;
    }

    message Segment {
      optional int64 id = 1;
      optional int64 resource_state = 2;
      optional string name = 3;
      optional string activity_type = 4;
      optional double distance = 5;
      optional double average_grade = 6;
      optional double maximum_grade = 7;
      optional double elevation_high = 8;
      optional double elevation_low = 9;
      repeated double start_latlng = 10;
      repeated double end_latlng = 11;
      optional int64 climb_category = 12;
      optional string city = 13;
      optional string state = 14;
      optional string country = 15;
      optional bool private = 16;
      optional bool hazardous = 17;
      optional bool starred = 18;
    }

    optional int64 id = 1;
    optional int64 resource_state = 2;
    optional string name = 3;
    optional Activity activity = 4;
    optional Athlete athlete = 5;
    optional int64 elapsed_time = 6;
    optional int64 moving_time = 7;
    optional string start_date = 8; // BQ TIMESTAMP — RFC 3339 string; Pub/Sub maps string to a TIMESTAMP column
    optional string start_date_local = 9; // BQ TIMESTAMP — RFC 3339 string; Pub/Sub maps string to a TIMESTAMP column
    optional double distance = 10;
    optional int64 start_index = 11;
    optional int64 end_index = 12;
    optional double average_cadence = 13;
    optional bool device_watts = 14;
    optional double average_watts = 15;
    optional Segment segment = 16;
    optional int64 kom_rank = 17;
    optional int64 pr_rank = 18;
    optional bool hidden = 19;
  }

  message SplitsMetric {
    optional double distance = 1;
    optional int64 elapsed_time = 2;
    optional double elevation_difference = 3;
    optional int64 moving_time = 4;
    optional int64 split = 5;
    optional double average_speed = 6;
    optional int64 pace_zone = 7;
  }

  message SplitsStandard {
    optional double distance = 1;
    optional int64 elapsed_time = 2;
    optional double elevation_difference = 3;
    optional int64 moving_time = 4;
    optional int64 split = 5;
    optional double average_speed = 6;
    optional int64 pace_zone = 7;
  }

  message Laps {
    message Activity {
      optional int64 id = 1;
      optional int64 resource_state = 2;
    }

    message Athlete {
      optional int64 id = 1;
      optional int64 resource_state = 2;
    }

    optional int64 id = 1;
    optional int64 resource_state = 2;
    optional string name = 3;
    optional Activity activity = 4;
    optional Athlete athlete = 5;
    optional int64 elapsed_time = 6;
    optional int64 moving_time = 7;
    optional string start_date = 8; // BQ TIMESTAMP — RFC 3339 string; Pub/Sub maps string to a TIMESTAMP column
    optional string start_date_local = 9; // BQ TIMESTAMP — RFC 3339 string; Pub/Sub maps string to a TIMESTAMP column
    optional double distance = 10;
    optional int64 start_index = 11;
    optional int64 end_index = 12;
    optional double total_elevation_gain = 13;
    optional double average_speed = 14;
    optional double max_speed = 15;
    optional double average_cadence = 16;
    optional bool device_watts = 17;
    optional double average_watts = 18;
    optional int64 lap_index = 19;
    optional int64 split = 20;
  }

  message BestEfforts {
    message Activity {
      optional int64 id = 1;
      optional int64 resource_state = 2;
    }

    message Athlete {
      optional int64 id = 1;
      optional int64 resource_state = 2;
    }

    message Segment {
      optional int64 id = 1;
      optional int64 resource_state = 2;
      optional string name = 3;
      optional string activity_type = 4;
      optional double distance = 5;
      optional double average_grade = 6;
      optional double maximum_grade = 7;
      optional double elevation_high = 8;
      optional double elevation_low = 9;
      repeated double start_latlng = 10;
      repeated double end_latlng = 11;
      optional int64 climb_category = 12;
      optional string city = 13;
      optional string state = 14;
      optional string country = 15;
      optional bool private = 16;
      optional bool hazardous = 17;
      optional bool starred = 18;
    }

    optional int64 id = 1;
    optional int64 resource_state = 2;
    optional string name = 3;
    optional Activity activity = 4;
    optional Athlete athlete = 5;
    optional int64 elapsed_time = 6;
    optional int64 moving_time = 7;
    optional string start_date = 8; // BQ TIMESTAMP — RFC 3339 string; Pub/Sub maps string to a TIMESTAMP column
    optional string start_date_local = 9; // BQ TIMESTAMP — RFC 3339 string; Pub/Sub maps string to a TIMESTAMP column
    optional double distance = 10;
    optional int64 start_index = 11;
    optional int64 end_index = 12;
    optional double average_cadence = 13;
    optional bool device_watts = 14;
    optional double average_watts = 15;
    optional Segment segment = 16;
    optional int64 kom_rank = 17;
    optional int64 pr_rank = 18;
    optional bool hidden = 19;
  }

  message StatsVisibility {
    optional string type = 1;
    optional string visibility = 2;
  }

  // Strava activity ID (primary key)
  optional int64 id = 1;
  // External activity ID
  optional string external_id = 2;
  // Strava upload ID
  optional int64 upload_id = 3;
  // Athlete information
  optional Athlete athlete = 4;
  // Activity name/title
  optional string name = 5;
  // Activity distance in meters
  optional double distance = 6;
  // Moving time in seconds
  optional int64 moving_time = 7;
  // Total elapsed time in seconds
  optional int64 elapsed_time = 8;
  // Total elevation gain in meters
  optional double total_elevation_gain = 9;
  // Highest elevation in meters
  optional double elev_high = 10;
  // Lowest elevation in meters
  optional double elev_low = 11;
  // Type of activity (Ride, VirtualRide, etc.)
  optional string type = 12;
  // Sport type classification
  optional string sport_type = 13;
  // Activity start time (UTC)
  optional string start_date = 14; // BQ TIMESTAMP — RFC 3339 string; Pub/Sub maps string to a TIMESTAMP column
  // Activity start time in local timezone
  optional string start_date_local = 15; // BQ TIMESTAMP — RFC 3339 string; Pub/Sub maps string to a TIMESTAMP column
  // Timezone of the activity
  optional string timezone = 16;
  // Starting latitude and longitude coordinates
  repeated double start_latlng = 17;
  // Ending latitude and longitude coordinates
  repeated double end_latlng = 18;
  // Number of achievements earned
  optional int64 achievement_count = 19;
  // Number of kudos received
  optional int64 kudos_count = 20;
  // Number of comments
  optional int64 comment_count = 21;
  // Number of athletes
  optional int64 athlete_count = 22;
  // Number of photos
  optional int64 photo_count = 23;
  // Total photo count
  optional int64 total_photo_count = 24;
  // Map data with polylines (polyline field not available from list endpoint)
  optional Map map = 25;
  // Whether activity was on a trainer
  optional bool trainer = 26;
  // Whether activity was a commute
  optional bool commute = 27;
  // Whether activity was manually entered
  optional bool manual = 28;
  // Whether activity is private
  optional bool private = 29;
  // Whether activity was flagged
  optional bool flagged = 30;
  // Type of workout
  optional string workout_type = 31;
  // Upload ID as string
  optional string upload_id_str = 32;
  // Average speed in m/s
  optional double average_speed = 33;
  // Maximum speed in m/s
  optional double max_speed = 34;
  // Whether user has kudoed this activity
  optional bool has_kudoed = 35;
  // Whether to hide from home feed (not available from list endpoint)
  optional bool hide_from_home = 36;
  // ID of gear used
  optional string gear_id = 37;
  // Total energy output in kilojoules
  optional double kilojoules = 38;
  // Average power output in watts
  optional double average_watts = 39;
  // Whether watts are from device
  optional bool device_watts = 40;
  // Maximum power output in watts
  optional int64 max_watts = 41;
  // Weighted average power in watts
  optional int64 weighted_average_watts = 42;
  // Activity description
  optional string description = 43;
  // Photo information (not available from list endpoint)
  optional Photos photos = 44;
  // Gear information
  optional Gear gear = 45;
  // Calories burned (may be null from list endpoint)
  optional double calories = 46;
  // Segment efforts data
  repeated SegmentEfforts segment_efforts = 47;
  // Device name
  optional string device_name = 48;
  // Embed token (not available from list endpoint)
  optional string embed_token = 49;
  // Metric splits data
  repeated SplitsMetric splits_metric = 50;
  // Standard splits data
  repeated SplitsStandard splits_standard = 51;
  // Lap data
  repeated Laps laps = 52;
  // Best efforts data
  repeated BestEfforts best_efforts = 53;
  // Average cadence
  optional double average_cadence = 54;
  // Whether activity has heartrate data
  optional bool has_heartrate = 55;
  // Number of personal records
  optional int64 pr_count = 56;
  // Strava suffer score
  optional double suffer_score = 57;
  // Stats visibility settings
  repeated StatsVisibility stats_visibility = 58;
  // Whether to display hide heartrate option
  optional bool display_hide_heartrate_option = 59;
  // Whether user opted out of heartrate
  optional bool heartrate_opt_out = 60;
  // Average heart rate in beats per minute
  optional double average_heartrate = 61;
  // Maximum heart rate in beats per minute
  optional double max_heartrate = 62;
  // Available zones
  repeated string available_zones = 63;
  // Activity visibility setting
  optional string visibility = 64;
  // CDC operation: UPSERT or DELETE
  optional string _CHANGE_TYPE = 65;
  // CDC ordering key; hex sections compared as unsigned numbers
  optional string _CHANGE_SEQUENCE_NUMBER = 66;
}
