// Code generated by schemas/bigquery/generate_proto.py. DO NOT EDIT.
// Source: schemas/bigquery/activities_full.json
//
// This proto mirrors the BigQuery `activities` table schema for use
// with the BigQuery Storage Write API. Field numbering is sequential
// per-message and is stable within a single regeneration; the API
// matches by name, not number, so reordering is safe.
//
// Regenerate via: just generate-bq-proto
// CI checks drift via: just verify-schemas

syntax = "proto3";

package desirelines.bigquery.v1;

// `go_package` is required by protoc-gen-go even though we don't
// consume the Go output today. Points at the stravapipe generated
// dir to match the Python output's conceptual location.
option go_package = "github.com/andy-esch/desirelines/packages/stravapipe/types/generated";

message Activity {
  message Athlete {
    // Athlete ID
    int64 id = 1;
    // Resource state
    int64 resource_state = 2;
  }

  message Map {
    // Map ID
    string id = 1;
    // Encoded polyline (not available from list endpoint)
    optional string polyline = 2;
    // Resource state
    int64 resource_state = 3;
    // Summary encoded polyline
    string summary_polyline = 4;
  }

  message Photos {
    message Primary {
      // Photo ID
      optional string id = 1;
      // Media type
      optional int64 media_type = 2;
      // Photo source
      int64 source = 3;
      // Unique photo ID
      string unique_id = 4;
      // Photo URLs as JSON
      string urls = 5;  // BQ JSON — JSON-encoded string
    }

    // Primary photo
    Primary primary = 1;
    // Total photo count
    int64 count = 2;
  }

  message Gear {
    // Gear ID
    string id = 1;
    // Whether this is primary gear
    bool primary = 2;
    // Gear name
    string name = 3;
    // Resource state
    int64 resource_state = 4;
    // Total distance on gear
    double distance = 5;
  }

  message SegmentEfforts {
    message Activity {
      int64 id = 1;
      int64 resource_state = 2;
    }

    message Athlete {
      int64 id = 1;
      int64 resource_state = 2;
    }

    message Segment {
      int64 id = 1;
      int64 resource_state = 2;
      string name = 3;
      string activity_type = 4;
      double distance = 5;
      double average_grade = 6;
      double maximum_grade = 7;
      double elevation_high = 8;
      double elevation_low = 9;
      repeated double start_latlng = 10;
      repeated double end_latlng = 11;
      int64 climb_category = 12;
      optional string city = 13;
      optional string state = 14;
      optional string country = 15;
      bool private = 16;
      bool hazardous = 17;
      bool starred = 18;
    }

    int64 id = 1;
    int64 resource_state = 2;
    string name = 3;
    Activity activity = 4;
    Athlete athlete = 5;
    int64 elapsed_time = 6;
    int64 moving_time = 7;
    int64 start_date = 8;  // BQ TIMESTAMP — micros since epoch
    int64 start_date_local = 9;  // BQ TIMESTAMP — micros since epoch
    double distance = 10;
    int64 start_index = 11;
    int64 end_index = 12;
    optional double average_cadence = 13;
    optional bool device_watts = 14;
    optional double average_watts = 15;
    Segment segment = 16;
    optional int64 kom_rank = 17;
    optional int64 pr_rank = 18;
    optional bool hidden = 19;
  }

  message SplitsMetric {
    double distance = 1;
    int64 elapsed_time = 2;
    optional double elevation_difference = 3;
    int64 moving_time = 4;
    int64 split = 5;
    double average_speed = 6;
    int64 pace_zone = 7;
  }

  message SplitsStandard {
    double distance = 1;
    int64 elapsed_time = 2;
    optional double elevation_difference = 3;
    int64 moving_time = 4;
    int64 split = 5;
    double average_speed = 6;
    int64 pace_zone = 7;
  }

  message Laps {
    message Activity {
      int64 id = 1;
      int64 resource_state = 2;
    }

    message Athlete {
      int64 id = 1;
      int64 resource_state = 2;
    }

    int64 id = 1;
    int64 resource_state = 2;
    string name = 3;
    Activity activity = 4;
    Athlete athlete = 5;
    int64 elapsed_time = 6;
    int64 moving_time = 7;
    int64 start_date = 8;  // BQ TIMESTAMP — micros since epoch
    int64 start_date_local = 9;  // BQ TIMESTAMP — micros since epoch
    double distance = 10;
    int64 start_index = 11;
    int64 end_index = 12;
    optional double total_elevation_gain = 13;
    double average_speed = 14;
    double max_speed = 15;
    optional double average_cadence = 16;
    optional bool device_watts = 17;
    optional double average_watts = 18;
    int64 lap_index = 19;
    int64 split = 20;
  }

  message BestEfforts {
    message Activity {
      int64 id = 1;
      int64 resource_state = 2;
    }

    message Athlete {
      int64 id = 1;
      int64 resource_state = 2;
    }

    message Segment {
      int64 id = 1;
      int64 resource_state = 2;
      string name = 3;
      string activity_type = 4;
      double distance = 5;
      double average_grade = 6;
      double maximum_grade = 7;
      double elevation_high = 8;
      double elevation_low = 9;
      repeated double start_latlng = 10;
      repeated double end_latlng = 11;
      int64 climb_category = 12;
      optional string city = 13;
      optional string state = 14;
      optional string country = 15;
      bool private = 16;
      bool hazardous = 17;
      bool starred = 18;
    }

    int64 id = 1;
    int64 resource_state = 2;
    string name = 3;
    Activity activity = 4;
    Athlete athlete = 5;
    int64 elapsed_time = 6;
    int64 moving_time = 7;
    int64 start_date = 8;  // BQ TIMESTAMP — micros since epoch
    int64 start_date_local = 9;  // BQ TIMESTAMP — micros since epoch
    double distance = 10;
    int64 start_index = 11;
    int64 end_index = 12;
    optional double average_cadence = 13;
    optional bool device_watts = 14;
    optional double average_watts = 15;
    Segment segment = 16;
    optional int64 kom_rank = 17;
    optional int64 pr_rank = 18;
    optional bool hidden = 19;
  }

  message StatsVisibility {
    string type = 1;
    string visibility = 2;
  }

  // Strava activity ID (primary key)
  int64 id = 1;
  // External activity ID
  optional string external_id = 2;
  // Strava upload ID
  optional int64 upload_id = 3;
  // Athlete information
  Athlete athlete = 4;
  // Activity name/title
  string name = 5;
  // Activity distance in meters
  double distance = 6;
  // Moving time in seconds
  int64 moving_time = 7;
  // Total elapsed time in seconds
  int64 elapsed_time = 8;
  // Total elevation gain in meters
  double total_elevation_gain = 9;
  // Highest elevation in meters
  optional double elev_high = 10;
  // Lowest elevation in meters
  optional double elev_low = 11;
  // Type of activity (Ride, VirtualRide, etc.)
  string type = 12;
  // Sport type classification
  string sport_type = 13;
  // Activity start time (UTC)
  int64 start_date = 14;  // BQ TIMESTAMP — micros since epoch
  // Activity start time in local timezone
  int64 start_date_local = 15;  // BQ TIMESTAMP — micros since epoch
  // Timezone of the activity
  string timezone = 16;
  // Starting latitude and longitude coordinates
  repeated double start_latlng = 17;
  // Ending latitude and longitude coordinates
  repeated double end_latlng = 18;
  // Number of achievements earned
  int64 achievement_count = 19;
  // Number of kudos received
  int64 kudos_count = 20;
  // Number of comments
  int64 comment_count = 21;
  // Number of athletes
  int64 athlete_count = 22;
  // Number of photos
  int64 photo_count = 23;
  // Total photo count
  int64 total_photo_count = 24;
  // Map data with polylines (polyline field not available from list endpoint)
  Map map = 25;
  // Whether activity was on a trainer
  bool trainer = 26;
  // Whether activity was a commute
  bool commute = 27;
  // Whether activity was manually entered
  bool manual = 28;
  // Whether activity is private
  bool private = 29;
  // Whether activity was flagged
  bool flagged = 30;
  // Type of workout
  optional string workout_type = 31;
  // Upload ID as string
  optional string upload_id_str = 32;
  // Average speed in m/s
  double average_speed = 33;
  // Maximum speed in m/s
  double max_speed = 34;
  // Whether user has kudoed this activity
  bool has_kudoed = 35;
  // Whether to hide from home feed (not available from list endpoint)
  optional bool hide_from_home = 36;
  // ID of gear used
  optional string gear_id = 37;
  // Total energy output in kilojoules
  optional double kilojoules = 38;
  // Average power output in watts
  optional double average_watts = 39;
  // Whether watts are from device
  optional bool device_watts = 40;
  // Maximum power output in watts
  optional int64 max_watts = 41;
  // Weighted average power in watts
  optional int64 weighted_average_watts = 42;
  // Activity description
  optional string description = 43;
  // Photo information (not available from list endpoint)
  Photos photos = 44;
  // Gear information
  Gear gear = 45;
  // Calories burned (may be null from list endpoint)
  optional double calories = 46;
  // Segment efforts data
  repeated SegmentEfforts segment_efforts = 47;
  // Device name
  optional string device_name = 48;
  // Embed token (not available from list endpoint)
  optional string embed_token = 49;
  // Metric splits data
  repeated SplitsMetric splits_metric = 50;
  // Standard splits data
  repeated SplitsStandard splits_standard = 51;
  // Lap data
  repeated Laps laps = 52;
  // Best efforts data
  repeated BestEfforts best_efforts = 53;
  // Average cadence
  optional double average_cadence = 54;
  // Whether activity has heartrate data
  bool has_heartrate = 55;
  // Number of personal records
  int64 pr_count = 56;
  // Strava suffer score
  optional double suffer_score = 57;
  // Stats visibility settings
  repeated StatsVisibility stats_visibility = 58;
  // Whether to display hide heartrate option
  optional bool display_hide_heartrate_option = 59;
  // Whether user opted out of heartrate
  optional bool heartrate_opt_out = 60;
  // Average heart rate in beats per minute
  optional double average_heartrate = 61;
  // Maximum heart rate in beats per minute
  optional double max_heartrate = 62;
  // Available zones
  repeated string available_zones = 63;
  // Activity visibility setting
  optional string visibility = 64;
}
