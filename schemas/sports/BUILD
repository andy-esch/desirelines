# Sport configuration schema
# Source of truth for sport types, metrics, and categorization
#
# After modifying sport_types.json, run: make sync-schemas
# This copies to packages that need it (stravapipe, apigateway)

files(
    name="config",
    sources=["sport_types.json"],
)
