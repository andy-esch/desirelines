# Production environment configuration (for Firebase Hosting prod deployment)
# This file is committed to git and contains non-sensitive defaults
# Smart mode: Anonymous users see fixtures, authenticated users see backend data
# Used by: vite build (default production mode)

# Disable fixture-only mode (will check auth state at runtime)
VITE_USE_FIXTURES=false

# API Gateway URL for authenticated users (prod environment)
# TODO: Replace with actual prod API Gateway URL from terraform outputs
VITE_API_GATEWAY_URL=https://YOUR-PROD-API-GATEWAY-URL.run.app
