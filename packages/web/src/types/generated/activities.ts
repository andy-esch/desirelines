// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: desirelines/activities/v1/activities.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "desirelines.activities.v1";

/**
 * Activity represents a single activity with full details.
 * Used for GET /activities/{id} responses.
 */
export interface Activity {
  /** Strava activity ID. */
  id: string;
  /** Activity name/title. */
  name: string;
  /**
   * Strava 'type' field: broad/deprecated category (Run, Ride, Workout, Swim, etc.).
   * Note: multiple sport_types share the same type (e.g., Yoga → type "Workout").
   */
  type: string;
  /**
   * App sport category (running, cycling, yoga).
   * Mapped from Strava's sport_type via sport_types.json config.
   */
  sport: string;
  /** Local start time as ISO 8601 timestamp. */
  startDateLocal: string;
  /** Distance in meters. */
  distanceMeters: number;
  /** Moving time in seconds. */
  movingTimeSeconds: number;
  /** Total elapsed time in seconds. */
  elapsedTimeSeconds: number;
  /** Elevation gain in meters (nullable). */
  elevationMeters?:
    | number
    | undefined;
  /** Average speed in m/s (nullable). */
  averageSpeedMps?:
    | number
    | undefined;
  /** Max speed in m/s (nullable). */
  maxSpeedMps?:
    | number
    | undefined;
  /** Average heart rate in bpm (nullable). */
  averageHeartrate?:
    | number
    | undefined;
  /** Max heart rate in bpm (nullable). */
  maxHeartrate?: number | undefined;
}

/**
 * ActivitySummary represents a condensed activity for list views.
 * Omits detailed stats not needed in list/table displays.
 */
export interface ActivitySummary {
  /** Strava activity ID. */
  id: string;
  /** Activity name/title. */
  name: string;
  /** Strava 'type' field: broad/deprecated category (Run, Ride, Workout, Swim, etc.). */
  type: string;
  /**
   * App sport category (running, cycling, yoga).
   * Mapped from Strava's sport_type via sport_types.json config.
   */
  sport: string;
  /** Local start time as ISO 8601 timestamp. */
  startDateLocal: string;
  /** Distance in meters. */
  distanceMeters: number;
  /** Moving time in seconds. */
  movingTimeSeconds: number;
  /** Elevation gain in meters (nullable). */
  elevationMeters?:
    | number
    | undefined;
  /**
   * True when the activity has real-world geography and thus appears on the
   * routes map: it is tagged to >=1 region (the same inclusion rule as
   * GET /activities/map/dataset). Indoor/virtual activities have no region tag
   * and are false. Drives the "view on map" affordance in list views.
   */
  hasRoute: boolean;
}

/**
 * MapActivity is a single geo-bearing activity for the routes-map cross-filter
 * dataset: scalar attributes plus the region tag ids it belongs to. Returned by
 * GET /activities/map/dataset. Only activities with >=1 region tag are included
 * (virtual/indoor activities are excluded), matching the /map/tiles and
 * /map/regions inclusion rule.
 */
export interface MapActivity {
  /** Strava activity ID. */
  activityId: string;
  /** Activity name/title (for the cross-filter activity list + click popover). */
  name: string;
  /**
   * App sport category (running, cycling, yoga).
   * Mapped from Strava's sport_type via sport_types.json config.
   */
  sport: string;
  /** Distance in meters. */
  distanceMeters: number;
  /** Moving time in seconds. */
  movingTime: number;
  /** Elevation gain in meters (nullable). */
  elevationMeters?:
    | number
    | undefined;
  /** Local start time as ISO 8601 timestamp. */
  startDateLocal: string;
  /**
   * Region ids this activity is tagged to. Same ids as GET /map/regions
   * returns (the desirelines.regions surrogate id), so the client can label
   * and filter consistently.
   */
  regionIds: string[];
  /**
   * Optional per-activity bounding box [minLng, minLat, maxLng, maxLat] from
   * the activity's route geometry, for map "fit to results". Absent when the
   * activity has no stored route geometry.
   */
  bbox: number[];
}

/**
 * MapDatasetResponse wraps the full set of geo-bearing activities for the
 * routes-map cross-filter model. No pagination: the client holds the whole set
 * and filters it in-memory.
 */
export interface MapDatasetResponse {
  /** All geo-bearing activities with scalars + region tag ids. */
  activities: MapActivity[];
}

/**
 * ListActivitiesRequest contains filters for listing activities.
 * Used for GET /activities endpoint.
 */
export interface ListActivitiesRequest {
  /** Start date filter (YYYY-MM-DD), inclusive. */
  from?:
    | string
    | undefined;
  /** End date filter (YYYY-MM-DD), inclusive. */
  to?:
    | string
    | undefined;
  /** Maximum number of results to return (default: 50, max: 100). */
  limit?:
    | number
    | undefined;
  /** Pagination cursor from previous response. */
  cursor?:
    | string
    | undefined;
  /**
   * Sport-category filter (e.g. cycling, running). Empty = all sports.
   * At most 20 categories per request.
   */
  sports: string[];
}

/** ListActivitiesResponse contains a paginated list of activities. */
export interface ListActivitiesResponse {
  /** List of activity summaries. */
  activities: ActivitySummary[];
  /**
   * Opaque cursor for fetching the next page.
   * Empty/absent if no more results.
   */
  nextCursor?:
    | string
    | undefined;
  /** True if more results are available. */
  hasMore: boolean;
}

/**
 * AggregateActivitiesRequest contains filters for the monthly activity
 * aggregate. Used for GET /activities/summary.
 */
export interface AggregateActivitiesRequest {
  /** Start date filter (YYYY-MM-DD), inclusive. */
  from?:
    | string
    | undefined;
  /** End date filter (YYYY-MM-DD), inclusive. */
  to?:
    | string
    | undefined;
  /**
   * Sport-category filter (e.g. cycling, running). Empty = all sports.
   * At most 20 categories per request.
   */
  sports: string[];
}

/**
 * ActivityBucket is one aggregated (month x sport x geographic) group with its
 * summed measures — the exact shape the charts view consumes.
 */
export interface ActivityBucket {
  /** Calendar month "YYYY-MM" in the athlete's local time (no timezone shift). */
  month: string;
  /** App sport category (running, cycling, yoga). */
  sport: string;
  /**
   * True when the activity has real-world geography: no trainer/manual flag,
   * not a Virtual type, and stored route geometry exists.
   */
  geographic: boolean;
  /** Number of activities in the group. */
  count: number;
  /**
   * Summed moving time in seconds. int32 (not int64) so protojson emits a
   * JSON number rather than a string; 2^31 seconds is ~68 years of moving
   * time, far beyond any real bucket.
   */
  movingTimeSeconds: number;
  /** Summed distance in meters. Zero for distance-less sports. */
  distanceMeters: number;
}

/**
 * AggregateActivitiesResponse contains the monthly buckets, sorted by month,
 * then sport, then geographic (false before true).
 */
export interface AggregateActivitiesResponse {
  buckets: ActivityBucket[];
}

/**
 * GetActivityRequest identifies a single activity to retrieve.
 * Used for GET /activities/{id} endpoint.
 */
export interface GetActivityRequest {
  /** Strava activity ID. */
  id: string;
}

/**
 * GetActivityResponse wraps the activity response.
 * Note: Could return Activity directly, but wrapper allows future metadata.
 */
export interface GetActivityResponse {
  /** The requested activity. */
  activity?: Activity | undefined;
}

function createBaseActivity(): Activity {
  return {
    id: "0",
    name: "",
    type: "",
    sport: "",
    startDateLocal: "",
    distanceMeters: 0,
    movingTimeSeconds: 0,
    elapsedTimeSeconds: 0,
    elevationMeters: undefined,
    averageSpeedMps: undefined,
    maxSpeedMps: undefined,
    averageHeartrate: undefined,
    maxHeartrate: undefined,
  };
}

export const Activity: MessageFns<Activity> = {
  encode(message: Activity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "0") {
      writer.uint32(8).int64(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.sport !== "") {
      writer.uint32(34).string(message.sport);
    }
    if (message.startDateLocal !== "") {
      writer.uint32(42).string(message.startDateLocal);
    }
    if (message.distanceMeters !== 0) {
      writer.uint32(49).double(message.distanceMeters);
    }
    if (message.movingTimeSeconds !== 0) {
      writer.uint32(56).int32(message.movingTimeSeconds);
    }
    if (message.elapsedTimeSeconds !== 0) {
      writer.uint32(64).int32(message.elapsedTimeSeconds);
    }
    if (message.elevationMeters !== undefined) {
      writer.uint32(73).double(message.elevationMeters);
    }
    if (message.averageSpeedMps !== undefined) {
      writer.uint32(81).double(message.averageSpeedMps);
    }
    if (message.maxSpeedMps !== undefined) {
      writer.uint32(89).double(message.maxSpeedMps);
    }
    if (message.averageHeartrate !== undefined) {
      writer.uint32(97).double(message.averageHeartrate);
    }
    if (message.maxHeartrate !== undefined) {
      writer.uint32(105).double(message.maxHeartrate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Activity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActivity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sport = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startDateLocal = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.distanceMeters = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.movingTimeSeconds = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.elapsedTimeSeconds = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 73) {
            break;
          }

          message.elevationMeters = reader.double();
          continue;
        }
        case 10: {
          if (tag !== 81) {
            break;
          }

          message.averageSpeedMps = reader.double();
          continue;
        }
        case 11: {
          if (tag !== 89) {
            break;
          }

          message.maxSpeedMps = reader.double();
          continue;
        }
        case 12: {
          if (tag !== 97) {
            break;
          }

          message.averageHeartrate = reader.double();
          continue;
        }
        case 13: {
          if (tag !== 105) {
            break;
          }

          message.maxHeartrate = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseActivitySummary(): ActivitySummary {
  return {
    id: "0",
    name: "",
    type: "",
    sport: "",
    startDateLocal: "",
    distanceMeters: 0,
    movingTimeSeconds: 0,
    elevationMeters: undefined,
    hasRoute: false,
  };
}

export const ActivitySummary: MessageFns<ActivitySummary> = {
  encode(message: ActivitySummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "0") {
      writer.uint32(8).int64(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.sport !== "") {
      writer.uint32(34).string(message.sport);
    }
    if (message.startDateLocal !== "") {
      writer.uint32(42).string(message.startDateLocal);
    }
    if (message.distanceMeters !== 0) {
      writer.uint32(49).double(message.distanceMeters);
    }
    if (message.movingTimeSeconds !== 0) {
      writer.uint32(56).int32(message.movingTimeSeconds);
    }
    if (message.elevationMeters !== undefined) {
      writer.uint32(65).double(message.elevationMeters);
    }
    if (message.hasRoute !== false) {
      writer.uint32(72).bool(message.hasRoute);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActivitySummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActivitySummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sport = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startDateLocal = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.distanceMeters = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.movingTimeSeconds = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 65) {
            break;
          }

          message.elevationMeters = reader.double();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.hasRoute = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseMapActivity(): MapActivity {
  return {
    activityId: "0",
    name: "",
    sport: "",
    distanceMeters: 0,
    movingTime: 0,
    elevationMeters: undefined,
    startDateLocal: "",
    regionIds: [],
    bbox: [],
  };
}

export const MapActivity: MessageFns<MapActivity> = {
  encode(message: MapActivity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activityId !== "0") {
      writer.uint32(8).int64(message.activityId);
    }
    if (message.name !== "") {
      writer.uint32(74).string(message.name);
    }
    if (message.sport !== "") {
      writer.uint32(18).string(message.sport);
    }
    if (message.distanceMeters !== 0) {
      writer.uint32(25).double(message.distanceMeters);
    }
    if (message.movingTime !== 0) {
      writer.uint32(32).int32(message.movingTime);
    }
    if (message.elevationMeters !== undefined) {
      writer.uint32(41).double(message.elevationMeters);
    }
    if (message.startDateLocal !== "") {
      writer.uint32(50).string(message.startDateLocal);
    }
    writer.uint32(58).fork();
    for (const v of message.regionIds) {
      writer.int64(v);
    }
    writer.join();
    writer.uint32(66).fork();
    for (const v of message.bbox) {
      writer.double(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MapActivity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMapActivity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.activityId = reader.int64().toString();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sport = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.distanceMeters = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.movingTime = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.elevationMeters = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.startDateLocal = reader.string();
          continue;
        }
        case 7: {
          if (tag === 56) {
            message.regionIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.regionIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 8: {
          if (tag === 65) {
            message.bbox.push(reader.double());

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.bbox.push(reader.double());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseMapDatasetResponse(): MapDatasetResponse {
  return { activities: [] };
}

export const MapDatasetResponse: MessageFns<MapDatasetResponse> = {
  encode(message: MapDatasetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.activities) {
      MapActivity.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MapDatasetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMapDatasetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.activities.push(MapActivity.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseListActivitiesRequest(): ListActivitiesRequest {
  return { from: undefined, to: undefined, limit: undefined, cursor: undefined, sports: [] };
}

export const ListActivitiesRequest: MessageFns<ListActivitiesRequest> = {
  encode(message: ListActivitiesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.from !== undefined) {
      writer.uint32(10).string(message.from);
    }
    if (message.to !== undefined) {
      writer.uint32(18).string(message.to);
    }
    if (message.limit !== undefined) {
      writer.uint32(32).int32(message.limit);
    }
    if (message.cursor !== undefined) {
      writer.uint32(42).string(message.cursor);
    }
    for (const v of message.sports) {
      writer.uint32(50).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListActivitiesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListActivitiesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.from = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.to = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.cursor = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sports.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseListActivitiesResponse(): ListActivitiesResponse {
  return { activities: [], nextCursor: undefined, hasMore: false };
}

export const ListActivitiesResponse: MessageFns<ListActivitiesResponse> = {
  encode(message: ListActivitiesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.activities) {
      ActivitySummary.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor !== undefined) {
      writer.uint32(18).string(message.nextCursor);
    }
    if (message.hasMore !== false) {
      writer.uint32(24).bool(message.hasMore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListActivitiesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListActivitiesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.activities.push(ActivitySummary.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextCursor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hasMore = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAggregateActivitiesRequest(): AggregateActivitiesRequest {
  return { from: undefined, to: undefined, sports: [] };
}

export const AggregateActivitiesRequest: MessageFns<AggregateActivitiesRequest> = {
  encode(message: AggregateActivitiesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.from !== undefined) {
      writer.uint32(10).string(message.from);
    }
    if (message.to !== undefined) {
      writer.uint32(18).string(message.to);
    }
    for (const v of message.sports) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AggregateActivitiesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAggregateActivitiesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.from = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.to = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sports.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseActivityBucket(): ActivityBucket {
  return { month: "", sport: "", geographic: false, count: 0, movingTimeSeconds: 0, distanceMeters: 0 };
}

export const ActivityBucket: MessageFns<ActivityBucket> = {
  encode(message: ActivityBucket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.month !== "") {
      writer.uint32(10).string(message.month);
    }
    if (message.sport !== "") {
      writer.uint32(18).string(message.sport);
    }
    if (message.geographic !== false) {
      writer.uint32(24).bool(message.geographic);
    }
    if (message.count !== 0) {
      writer.uint32(32).int32(message.count);
    }
    if (message.movingTimeSeconds !== 0) {
      writer.uint32(40).int32(message.movingTimeSeconds);
    }
    if (message.distanceMeters !== 0) {
      writer.uint32(49).double(message.distanceMeters);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActivityBucket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActivityBucket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.month = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sport = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.geographic = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.movingTimeSeconds = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.distanceMeters = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAggregateActivitiesResponse(): AggregateActivitiesResponse {
  return { buckets: [] };
}

export const AggregateActivitiesResponse: MessageFns<AggregateActivitiesResponse> = {
  encode(message: AggregateActivitiesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.buckets) {
      ActivityBucket.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AggregateActivitiesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAggregateActivitiesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.buckets.push(ActivityBucket.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseGetActivityRequest(): GetActivityRequest {
  return { id: "0" };
}

export const GetActivityRequest: MessageFns<GetActivityRequest> = {
  encode(message: GetActivityRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "0") {
      writer.uint32(8).int64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActivityRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActivityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseGetActivityResponse(): GetActivityResponse {
  return { activity: undefined };
}

export const GetActivityResponse: MessageFns<GetActivityResponse> = {
  encode(message: GetActivityResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activity !== undefined) {
      Activity.encode(message.activity, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActivityResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActivityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.activity = Activity.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
