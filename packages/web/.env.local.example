# Local development overrides (copy to .env.local and customize)
# .env.local is gitignored and takes precedence over all other env files
# Use this for local development with custom settings

# For local dev with fixtures (default)
VITE_USE_FIXTURES=true
VITE_API_GATEWAY_URL=http://localhost:8080

# Or, for local dev with real backend:
# VITE_USE_FIXTURES=false
# VITE_API_GATEWAY_URL=https://your-local-api-gateway.run.app
