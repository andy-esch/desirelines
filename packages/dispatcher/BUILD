# Webhook dispatcher Go package
# Routes Strava webhooks to PubSub

go_mod(
    name="mod",
)

go_package(
    name="lib",
)

# Docker builds handled by docker-compose for local dev
# docker_image(name="docker")
